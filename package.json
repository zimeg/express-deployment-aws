{
  "name": "serverless_express_example",
  "version": "0.0.2",
  "license": "ISC",
  "scripts": {
    "lint": "yarn run eslint ./",
    "lambda": "claudia test-lambda --event event.json",
    "update": "claudia update",
    "deploy": "claudia create --version dev --region us-east-1 --handler lambda.handler --role claudia-global"
  },
  "devDependencies": {
    "eslint": "^7.2.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1"
  },
  "dependencies": {}
}
